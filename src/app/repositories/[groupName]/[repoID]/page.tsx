// External Dependencies

// Relative Dependencies

type Props = {
  params: {
    repoID: string;
  };
};

const Page = async ({ params: { repoID } }: Props) => {
  return <div>Page</div>;
};

export default Page;
